import 'package:flutter/material.dart';

class WeatherAwarenessScreen extends StatefulWidget {
  const WeatherAwarenessScreen({super.key});

  @override
  State<WeatherAwarenessScreen> createState() => _WeatherAwarenessScreenState();
}

class _WeatherAwarenessScreenState extends State<WeatherAwarenessScreen> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
